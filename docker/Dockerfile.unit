FROM node:22-alpine
WORKDIR /app
RUN npm install -g npm@11
COPY package*.json ./
RUN npm install

COPY tsconfig.json vite.config.ts ./
COPY src/ ./src/

CMD ["npx", "vitest", "run", "--reporter=verbose"]
